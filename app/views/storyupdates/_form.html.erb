<h1><strong>Add to this story!</strong></h1>

<%= form_for @storyupdate, url: story_storyupdates_path(@story.id) do |f| %>

<% if @storyupdate.errors.any? %>
 <div id="error_explanation">
   <h2><%= pluralize(@storyupdate.errors.count, "error") %> prohibited
     this story from being saved:</h2>
   <ul>
   <% @storyupdate.errors.full_messages.each do |msg| %>
     <li><%= msg %></li>
   <% end %>
   </ul>
 </div>
<% end %>

 <p>
   <%= f.label :title %><br>
   <%= f.text_area :title %>
 </p>

 <p>
   <%= f.label :text %><br>
   <%= f.text_area :text %>
 </p>

 <p>
   <%= f.submit 'Save', class: 'btn btn-default' %>
 </p>
<% end %>