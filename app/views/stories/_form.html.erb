<h1><strong>Stories</strong></h1>

<%= form_for @story do |f| %>
<% if @story.errors.any? %>
 <div id="error_explanation">
   <h2><%= pluralize(@story.errors.count, "error") %> prohibited
     this story from being saved:</h2>
   <ul>
   <% @story.errors.full_messages.each do |msg| %>
     <li><%= msg %></li>
   <% end %>
   </ul>
 </div>
 <% end %>


 <p>
   <%= f.label :text %><br>
   <%= f.text_area :write %>
 </p>

 <p>
   <%= f.submit 'Save', class: 'btn btn-default' %>
 </p>
<% end %>